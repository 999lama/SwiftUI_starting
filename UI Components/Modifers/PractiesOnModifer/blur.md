
#### `.blur(radius: 5) `

```
 var body: some View {
        VStack {
            Text("Hello world")
        }
        .background(.blue)
        .blur(radius: 5)
        .ignoresSafeArea()
    }
```
    
* give some view a blur effect.
